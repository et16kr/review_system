diff --git a/tsrc/id/iduQueue/iduQueuePerf.cpp b/tsrc/id/iduQueue/iduQueuePerf.cpp
index 1111111..2222222 100644
--- a/tsrc/id/iduQueue/iduQueuePerf.cpp
+++ b/tsrc/id/iduQueue/iduQueuePerf.cpp
@@ -90,6 +90,16 @@ int main(int argc, char *argv[])
-    sQueueType = atoi(argv[1]);
+    IDE_RC  sRet;
+    sQueueType = atoi(argv[1]);
+    sEnqueueThread = (enqueueThread **)idlOS::malloc(sEnqueueThreadCount * sizeof(enqueueThread *));
+    sDequeueThread = (dequeueThread **)idlOS::malloc(sDequeueThreadCount * sizeof(dequeueThread *));
+    sEnqueueThread[0] = new enqueueThread();
+    delete sEnqueueThread[0];
+    idlOS::free(sEnqueueThread);
+    idlOS::printf("ENQUEUE[%3" ID_UINT32_FMT "]\n", 0);
